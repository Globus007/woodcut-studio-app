# Shop instruction is a sheet page, not the inspector

The carpenter reads and prints the shop instruction on its own page. The desk is the board and the pattern form; a footer glance (waste, blank size) is not the document. Ticket 10 put the sheet in the inspector Build tab so print CSS could target one node — that made a long unreadable column and duplicated the rail. Rejected: stuffing the same document into a wider inspector, a live pane on the stage, or a second PDF engine this pass. Browser print of the sheet page is the PDF artifact.
